services:
  web:
    image: ${GOODMONEYING_WEB_IMAGE}:${GOODMONEYING_IMAGE_TAG}
    container_name: goodmoneying-web
    restart: unless-stopped
    env_file:
      - /opt/goodmoneying/env/web.env
    ports:
      - "8080:80"
