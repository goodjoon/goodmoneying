# 백필 진행률 갱신과 재개/큐 전환 검증

## 대상

- Task: `docs/Task/M3-T16-2026-06-21-001-백필-진행률-갱신과-재개-큐-전환.md`
- 범위: Backfill 관리 화면 10초 갱신, `paused` 작업 재개 버튼, 대상 코인 전체 툴팁, 멈춤/중지 후 다음 백필 작업 큐 전환

## RED

- `npm --workspace apps/web run test -- src/useOperationsConsole.test.tsx src/App.test.tsx`
  - 결과: 실패
  - 확인: Backfill 관리 화면 10초 재조회 누락, 대상 코인 툴팁 누락
- `uv run pytest tests/worker/test_collector_behavior.py::test_worker_claims_next_pending_job_after_current_job_is_stopped -q`
  - 결과: 실패
  - 확인: 현재 작업 중지 후 다음 pending 작업이 같은 실행 틱에서 처리되지 않고 `written == 1`

## GREEN

- `npm --workspace apps/web run test -- src/useOperationsConsole.test.tsx src/App.test.tsx`
  - 결과: 통과, 2 files / 11 tests
- `uv run pytest tests/worker/test_collector_behavior.py -q`
  - 결과: 통과, 17 passed
- `uv run ruff check apps/worker/goodmoneying_worker/collector.py tests/worker/test_collector_behavior.py`
  - 결과: 통과

## 전체 검증

- `npm test`
  - 결과: 통과, 7 files / 30 tests
- `uv run pytest -q`
  - 결과: 통과, 96 passed, 1 warning
  - 경고: FastAPI TestClient의 StarletteDeprecationWarning
- `uv run mypy apps/api apps/worker packages/shared tests`
  - 결과: 통과, no issues found in 31 source files
- `npm run build`
  - 결과: 통과
- `uv run ruff check .`
  - 결과: 통과
- `npm run e2e`
  - 1차 결과: 실패
  - 원인: E2E 기대값이 백필 대상 코인 표시 순서를 생성 순서로 가정했지만 API는 `market_code` 순서로 반환
  - 조치: E2E 기대값을 API 반환 순서와 같은 `marketCode` 정렬로 수정
- `npm run e2e`
  - 최종 결과: 통과, Chromium 1 passed

## 수동 확인

- 별도 수동 확인 없음. Playwright E2E에서 Backfill 관리 진입, `paused` 작업 표시, 전체 대상 코인 툴팁, 재개 버튼 클릭, 백필 시작 흐름을 실제 브라우저로 확인했다.

## 남은 리스크

- 기본 브라우저 툴팁(title)은 접근성 보조 설명으로도 읽히지만, 커스텀 지연 툴팁 UI는 아니다. 화면 디자인 요구가 더 강해지면 별도 tooltip 컴포넌트로 분리한다.
